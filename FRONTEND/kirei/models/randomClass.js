class User {
    constructor({ picture: {large}, name, email} ) {
        this.picture = large;
        this.name = `${name.first} ${name.last}`;
        this.email = email;
    }
  }
  export default User;